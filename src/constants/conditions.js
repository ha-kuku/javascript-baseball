const USER_INPUT = object.set({
  replay: 1,
  end: 2,
});

const GAMEBOX_CONDITION = object.set({
  maxLength: 3,
  noInclusion: 0,
  startNumber: 1,
  endNumber: 9,
});

export { USER_INPUT, GAMEBOX_CONDITION };
